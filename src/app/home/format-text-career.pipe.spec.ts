import { FormatTextCareerPipe } from './format-text-career.pipe';

describe('FormatTextCareerPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTextCareerPipe();
    expect(pipe).toBeTruthy();
  });
});
